<% provide(:title, 'Edit user') %>
<% provide(:button_text, 'Save changes') %>

<h1 style="font-size: 70px; color: #057DCD; text-align: left">Update your profile</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
